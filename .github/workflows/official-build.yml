name: Build TalkBack via Official Partner Script
on:
  workflow_dispatch: # 支持手动点击运行
jobs:
  build-talkback:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      # 1. 准备 Java 17 环境 (脚本官方推荐版本)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # 3. 运行官方 build.sh 脚本
      - name: Execute build.sh
        run: |
          # 赋予脚本执行权限
          chmod +x ./build.sh          
          # 注入脚本要求的环境变量
          # $ANDROID_HOME 是 GitHub Runner 预装 SDK 的标准路径
          export ANDROID_SDK=$ANDROID_HOME          
          # 打印调试信息
          echo "🚀 开始构建..."
          echo "Using Android SDK at: $ANDROID_SDK"
          echo "Using Java Home at: $JAVA_HOME"          
          # 执行官方脚本
          ./build.sh
      # 4. 收集并上传生成的 APK
      - name: Upload TalkBack APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: TalkBack-Official-APK          
# 根据你上次运行生成的路径进行精准抓取
          path: |
            build/outputs/apk/phone/debug/talkback-phone-debug.apk
            build/outputs/apk/wear/debug/talkback-wear-debug.apk
          if-no-files-found: error
